language: node_js
node_js:
 - 12.19.0
cache: npm

install:
 - npm install

script:
  - npm test
  - npm run build

deploy:
 provider: pages
 skip-cleanup: true
 github-tocken: $GITHUB_TOKEN
 on:
  branch: gh-pages